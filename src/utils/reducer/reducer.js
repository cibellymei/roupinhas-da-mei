export const createAction = (type, payload) => ({ type, payload });
// Define uma função chamada `createAction` que cria um objeto de ação para ser usado com Redux.
// Recebe dois argumentos: `type` e `payload`.